# ── Python ───────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
dist2/
build/
*.spec
results/
# ── 發行包（make_release_zip.bat 產生，上傳至 GitHub Releases）──
QwenASR_*.zip
QwenASR_*.7z

# ── 虛擬環境 ─────────────────────────────────────────
venv/
venv-gpu/
build_venv/
.venv/
env/

# ── 模型檔案（下載後產生，體積大）──────────────────────
ov_models/qwen3_asr_int8/
ov_models/qwen3_asr_1p7b_int8/
ov_models/qwen3_asr_1p7b_kv/
ov_models/qwen3_asr_1p7b_kv_int8/
ov_models/qwen3_asr_1p7b_kv_int8_openvino_Scripts/
ov_models/silero_vad*.onnx
ov_models/*.gguf
onnx_models/
GPUModel/
GGUF/
ffmpeg/
*.onnx
*.bin
*.xml
*.gguf

# 圖示原始生成大圖（icon.ico / icon.png 為產出物，須追蹤；原圖不追蹤）
assets/icon_src.png

# ── 允許特定 ov_models 檔案 ───────────────────────────
!ov_models/mel_filters.npy

# ── 允許程式圖示 PNG（覆寫上方 *.png 全域忽略）─────────
!assets/icon.png
!webview/favicon.png

# ── 自動產生的設定/快取檔 ────────────────────────────
settings.json
settings-gpu.json

# ── 輸出檔案 ─────────────────────────────────────────
subtitles/
*.srt
installer/

# ── 所有 BAT 檔預設忽略，以下三個除外 ───────────────────
*.bat
!build.bat
!build_venv.bat
!start-gpu.bat
!make_release_zip.bat

# ── 開發/測試用腳本 ──────────────────────────────────
test_*.py
benchmark.py
check_*.py
export_onnx.*
generate_srt.py
merge_onnx.py
cleanup_merged.py
download_test_audio.py
quantize_cpu.py
REQ.txt
setup.iss

# ── 測試音檔 ─────────────────────────────────────────
test_audio/
*.wav
*.mp3
*.flac
*.m4a

# ── chatllm DLL 目錄（二進位，體積大，從 GitHub Releases 取得）────────
chatllm/
chatllmtest/

# ── cloudflared（對外通道二進位，隨用下載，不入庫）────────────────────
cloudflared/

# ── CrispASR / Whisper 核心與測試資料（隨用下載 / 開發測試，不入庫）──────
crispasr/
CASRCore/
_freshtest/
_freshtest_models/
crisp_smoke.py
compress_kv_int4.py
ov_models/qwen3_asr_1p7b_kv_int4/

# ── 雜項暫存（誤產生 / 工作交接，不入庫）────────────────────────────
*.stackdump
工作交接*.md
UI.txt
/root

# ── 其他 ─────────────────────────────────────────────
.claude/
*.jpg
*.png
*.log
*.tmp

# 程式圖示 PNG 例外（必須放在 *.png 之後才生效）
!assets/icon.png
!webview/favicon.png

# ── 允許 Readme 資料夾內的截圖（含 webview 子資料夾）──────
!Readme/*.jpg
!Readme/*.png
!Readme/webview/*.jpg
!Readme/webview/*.png
